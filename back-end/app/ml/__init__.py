"""Pipeline de ML/forecasting: dataset, modelos, avaliação, backtest e inferência.

Imports deste pacote não têm side effects (sem .env, sem rede) — conexões só
acontecem quando um componente roda de fato, igual ao resto do projeto.
"""
